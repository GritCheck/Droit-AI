# Cosmos DB Emulator override file
# Use with: docker-compose -f docker-compose.yml -f docker-compose.cosmos.yml up

version: '3.8'

services:
  backend:
    environment:
      # Override to use local Cosmos DB emulator
      - COSMOS_DB_ENDPOINT=https://cosmos-emulator:8081/
      - COSMOS_DB_KEY=C2y6yDjf5/R+ob0N8A7Cgv30VRDjWEJ/LJQW8wYf0nn4Wvfa2g17n/Gz5t3jaN5322Bla8+9rA3z7aE4HMw==  # Standard emulator key
    depends_on:
      - cosmos-emulator

  # Enable the cosmos-emulator service
  cosmos-emulator:
    profiles: []  # Remove from profile to start by default
